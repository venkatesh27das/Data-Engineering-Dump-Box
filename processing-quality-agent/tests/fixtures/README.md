# Synthetic fixture catalog

The in-memory repository supplies a digital/scanned policy PDF with an expected missing table and confidence disagreement. Unit tests construct corrupt-file, parser-timeout, recovery-success, and recovery-exhausted cases. Add JSON snapshots here for external SDK contract regression tests; never add PII or PHI.
